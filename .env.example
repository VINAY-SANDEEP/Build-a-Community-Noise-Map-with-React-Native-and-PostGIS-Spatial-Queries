# Example environment variables for Community Noise Map
# Copy to .env and adjust as needed

# Backend
PORT=3000

# Database
DB_HOST=db
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=noisemap
DB_PORT=5432

# For production, set secure values and do not commit real secrets to source control.
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=noisemap
PORT=3000
